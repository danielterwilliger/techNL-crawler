# Credentials & secrets — NEVER commit an LLM token. Per-operator only.
.env
.env.*
.active_token
.gemini/
**/.active_token

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.venv/

# Scratch / local
temp_*.txt
*.bak
dumped_text/
logs/

# Note: data/*.json (companies_state.json, open_jobs.json, known_jobs.json) IS
# committed on purpose — the JSON feed is the public output of this project.
